#!/bin/bash
"""
Securon CLI Wrapper Script

This script provides a convenient way to run the Securon CLI from anywhere.
"""

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Python CLI with all arguments passed through
python3 "$SCRIPT_DIR/securon_cli.py" "$@"